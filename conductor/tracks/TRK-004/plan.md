# Implementation Plan: Decision Engine

**Track ID:** TRK-004
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-13
**Status:** [x] Complete

## Overview

Three phases: Gemini client + option schema, rule engine + store integration, REST endpoints.

---

## Phase 1: Gemini Client + Option Generation

### Tasks

- [x] 1.1: Create `src/modules/decision-engine/gemini.ts` — builds prompt from shipment context, calls gemini-1.5-flash, parses structured JSON response into `DecisionOption[]`
- [x] 1.2: Add zod schema for validating Gemini response
- [x] 1.3: Mock fallback — 3 plausible options returned when API key missing or call fails

### Verification

- [x] Without API key, mock options returned with correct structure (Safe/Aggressive/Defer)
- [x] Zod schema validates Gemini response structure

---

## Phase 2: Rule Engine + Store Integration

### Tasks

- [x] 2.1: Create `src/modules/decision-engine/rule-engine.ts` — `shouldAutoExecute(option)` returns true if confidence > 0.85 AND cost_delta_usd < 50000 AND label != 'Defer'
- [x] 2.2: Create `src/modules/decision-engine/index.ts` — `generateDecision(shipmentId)` orchestrates: fetch shipment → call Gemini → evaluate options → auto-execute or queue
- [x] 2.3: Auto-executed decisions pushed to store with status='auto_executed' and audit-logged

### Verification

- [x] Aggressive option (confidence 0.92, $47k) auto-executes (status='auto_executed', resolved_by='auto')
- [x] Auto-executed decision appears in audit log immediately

---

## Phase 3: REST Endpoints

### Tasks

- [x] 3.1: Create `src/routes/decisions.ts` — all 5 endpoints (generate, list, audit, approve, reject)
- [x] 3.2: Mount decisions router in `src/index.ts` at `/api/v1/decisions`
- [x] 3.3: Add `rejectDecision(id)` to store — sets status='overridden', audit-logs

### Verification

- [x] `POST /api/v1/decisions/generate` with SH-4800 returns DecisionRecord with 3 options
- [x] `GET /api/v1/decisions` returns empty list (all auto-executed, none pending)
- [x] `GET /api/v1/decisions/audit` returns 1 entry after generate
- [x] `POST /api/v1/decisions/:id/approve` returns 404 for non-existent/non-pending
- [x] `POST /api/v1/decisions/:id/reject` returns 404 for non-existent/non-pending

---

## Final Verification

- [x] `npx tsc --noEmit` passes with zero errors
- [x] All acceptance criteria in spec.md met
- [x] Ready to begin TRK-005

---
_Generated by Conductor. Tasks marked [~] in progress, [x] complete._
