# Implementation Plan: Cascade Simulator

**Track ID:** TRK-003
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-13
**Status:** [x] Complete

## Overview

Three phases: BFS graph traversal, dollar exposure + report assembly, then REST endpoints and auto-trigger hook on disruption events.

---

## Phase 1: BFS Graph Traversal

### Tasks

- [x] 1.1: Create `src/modules/cascade-simulator/graph-traversal.ts` — BFS from trigger shipment, max 5 hops, 85% delay attenuation per hop, returns ordered list of `CascadeNode`
- [x] 1.2: Track visited set to prevent cycles, collect hop depth per node
- [x] 1.3: Track critical path — chain from trigger to the node with highest `sla_exposure_usd`

### Verification

- [x] BFS from SH-4800 returns SH-4810 (hop 1) per dependency graph
- [x] Delay attenuates: 18hrs → 15.3hrs at hop 1 → 13hrs at hop 2
- [x] No infinite loops on graph with 20 edges

---

## Phase 2: Exposure Calculation + Report Assembly

### Tasks

- [x] 2.1: Create `src/modules/cascade-simulator/exposure.ts` — for each cascade node, look up shipment SLA deadline + customer penalty rate, compute `sla_breached` and `sla_exposure_usd`
- [x] 2.2: Assemble `CascadeImpactReport` — de-duplicate `affected_purchase_orders` and `affected_customers`, sum total exposure, include `critical_path`
- [x] 2.3: Create `src/modules/cascade-simulator/index.ts` — exports `simulateCascade(shipmentId, delayHours)` and `getCascadeGraph()`

### Verification

- [x] Triggering SH-4800 with 18hr delay produces a report with 1 affected shipment (SH-4810)
- [x] `total_sla_exposure_usd` matches sum of individual node exposures
- [x] `critical_path` contains the trigger ID as first element

---

## Phase 3: REST Endpoints + Disruption Auto-Trigger

### Tasks

- [x] 3.1: Create `src/routes/cascade.ts` — `POST /api/v1/cascade/simulate` and `GET /api/v1/cascade/graph`
- [x] 3.2: Mount cascade router in `src/index.ts`
- [x] 3.3: Hook auto-trigger into `store.addDisruption()` — when a disruption is added, run `simulateCascade` for each affected shipment and broadcast the report via SSE

### Verification

- [x] `POST /api/v1/cascade/simulate` with `{ "shipmentId": "SH-4800", "delayHours": 18 }` returns valid `CascadeImpactReport`
- [x] `GET /api/v1/cascade/graph` returns adjacency list with 20 edges
- [x] `total_sla_exposure_usd` correctly computes $229,167 for SH-4800 with 500hr delay

---

## Final Verification

- [x] All acceptance criteria in spec.md are met
- [x] `npx tsc --noEmit` passes with zero errors
- [x] `GET /api/v1/health` still returns `{ status: "ok", shipmentCount: 40 }`
- [x] Ready to begin TRK-004 (Decision Engine)

---
_Generated by Conductor. Tasks marked [~] in progress, [x] complete._
